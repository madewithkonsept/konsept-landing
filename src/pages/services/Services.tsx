import { PageWrapper } from '@/components';
import { Center, Text } from '@chakra-ui/react';

export const Services = () => {
  return (
    <PageWrapper id={'services'}>
      <Center w={'full'} h={'100dvh'}>
        <Text fontSize={'4xl'}>CUMING SOON</Text>
      </Center>
    </PageWrapper>
  );
};
